global class ECM_PromoStandardPSGenerationSchedule implements Schedulable{
	global void execute(SchedulableContext sc) {

        //ECM_PromoStandardPSGenerationBatch b1 = new ECM_PromoStandardPSGenerationBatch();
        //Database.executeBatch(b1);           
    }
}